# QuantX Landing (FULL RUNNABLE) — Angular 20 + PrimeNG v20 + Tailwind (v4)

Fixes your latest npm error:
- `@primeuix/themes@^21.0.0` does **not** exist on npm.
- This project uses the current npm major: **@primeuix/themes ^2.0.2**.

## Clean install (recommended)
```bat
rmdir /s /q node_modules
del package-lock.json
npm cache verify
npm install
```

## Run
```bat
npm start
```

Open: http://localhost:4200
